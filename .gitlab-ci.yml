stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  before_script:
    - cp ${PROD_ENV} .env_prod
    - cp ${TEST_ENV} .env_test
  script:
    - docker build -t booking_back_image .


#test-job:
#  stage: test
#  script:
#    - echo "Hello World"

deploy-job:
  stage: deploy
  script:
    - docker compose up -d